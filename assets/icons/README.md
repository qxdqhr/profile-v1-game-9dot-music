# Icons

Minimal stroke icons for pause UI (`pause` / `play` / `restart` / `songs`).

Style inspired by [Lucide](https://lucide.dev/) (ISC). SVGs here are hand-authored for this game; safe for Web export without emoji fonts.
